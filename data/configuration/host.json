{
    "id": "host",
    "name": "Host",
    "description": "A host configuration object",
    "configuration": {
        "pes": {
            "description": "The host's number of processing elements",
            "type": "int",
            "defaultValue": 1,
            "required": true
        },
        "peProvisioner": {
            "description": "The provisioning policy used by a host to provide virtual processing elements (PEs) to its virtual machines",
            "type": "select",
            "defaultValue": "Simple",
            "required": true,
            "multiple": false,
            "options": [
                "Simple"
            ]
        },
        "mips": {
            "description": "The amount of mips per processing units",
            "type": "int",
            "defaultValue": 1000,
            "required": true
        },
        "vmScheduler": {
            "description": "",
            "type": "select",
            "defaultValue": "TimeShared",
            "required": true,
            "multiple": false,
            "options": [
                "TimeShared",
                "SpaceShared"
            ]
        },
        "ram": {
            "description": "The amount of RAM in Megabytes",
            "type": "int",
            "defaultValue": 1024,
            "required": true
        },
        "ramProvisioner": {
            "description": "The provisioning policy used by a host to provide RAM to its Vms",
            "type": "select",
            "defaultValue": "Simple",
            "required": true,
            "multiple": false,
            "options": [
                "Simple"
            ]
        },
        "bw": {
            "description": "The amount of Bandwidth in Megabytes",
            "type": "int",
            "defaultValue": 10000,
            "required": true
        },
        "bwProvisioner": {
            "description": "The provisioning policy used by a host to provide Bandwidth to its Vms",
            "type": "select",
            "defaultValue": "Simple",
            "required": true,
            "multiple": false,
            "options": [
                "Simple"
            ]
        },
        "storage": {
            "description": "The storage capacity in Megabytes",
            "type": "int",
            "defaultValue": 10000,
            "required": true
        }
    }
}
